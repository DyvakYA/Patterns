package specification;

public class Main {

    public static void main(String[] args) {
        // Specification spec = new AndSpecification();
        // spec.and(spec).or(spec).and(spec);
    }
}
